# Configuration serveur
PORT=5000
NODE_ENV=development

# Configuration JWT
JWT_SECRET=digitalcraft_secret_key_2025_changez_moi
JWT_EXPIRES_IN=24h

# Configuration base de données
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=digitalcraft

# Configuration CORS
CORS_ORIGIN=http://localhost:3000

# Configuration upload
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5MB

# Configuration email (optionnel)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=